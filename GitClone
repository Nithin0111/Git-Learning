to clone any git repository from github click on clone or download and copy the ssh code and 
go to terminal of the folder you want to come the files to type git clone (paste ssh code)
to get all files without any folder type (git clone (ssh code) .) and that's it peace bro
